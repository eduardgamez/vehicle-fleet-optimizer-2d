# Dependencias del pipeline. numpy y numba las necesita el planificador; torch,
# el entrenamiento; y optuna, la búsqueda guiada y la fase 3.
#
# Las ruedas de torch traen dentro las librerías de CUDA, así que la máquina solo
# necesita el driver de NVIDIA: no hace falta una imagen base con CUDA.
numpy==2.4.2
numba==0.66.0
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0
optuna==4.9.0
